# Solana private key (base58-encoded, 64-byte keypair)
# This is the agent's wallet that will pay for x402 API calls.
# Needs: USDC (SPL token) + small SOL for fees on Solana mainnet.
SOLANA_PRIVATE_KEY=your-base58-encoded-keypair-here
